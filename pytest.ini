[pytest]
addopts = -ra
testpaths = tests
python_files = test_*.py
norecursedirs =
  refs
  _iso
  data
  datasets
  squashfs-root
  build
  dist
  target
  .venv
markers =
  integration: requires local large assets (ISO/data/replays)

