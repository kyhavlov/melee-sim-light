#ifndef MSL_CORE_PLATFORM_TRIGF_H
#define MSL_CORE_PLATFORM_TRIGF_H

// Original files sometimes include the GameCube MSL header without its MSL/
// prefix. Hosted libc supplies the same public single-precision entry points.
#include <math.h>

#endif
