--- a/src/melee/ft/fighter.c
+++ b/src/melee/ft/fighter.c
@@ -182,9 +182,14 @@
     ftCo_8009F4A4();
 #endif
     ftCo_800C8064();
+#ifndef MSL_CORE_HOSTED
+    // This source owner only installs the alternate renderer material used by
+    // ftMaterial_800BF2B8. The headless runtime has no fighter draw traversal.
+    // refs/melee/src/melee/ft/ft_0C8C.c::ftCo_800C8F6C
     ftCo_800C8F6C(); ///< @todo &fighter_alloc_data+2, +3, +4 are not defined
                      ///< in the fighter.s
     // data section, how does this work?
+#endif
     HSD_ObjAllocInit(&fighter_parts_alloc_data, /*size*/ 0x8c0, /*align*/ 4);
     HSD_ObjAllocInit(&fighter_dobj_list_alloc_data, /*size*/ 0x1f0,
                      /*align*/ 4);
@@ -908,7 +913,12 @@
     ftData_80085B10(fp);
     ftParts_80074E58(fp);
     ftParts_SetupParts(gobj);
+#ifndef MSL_CORE_HOSTED
+    // Costume MObj/TObj animation is renderer-only. The gameplay skeleton and
+    // Figa animation owners are initialized by the surrounding calls.
+    // refs/melee/src/melee/ft/ftanim.c::ftAnim_80070308
     ftAnim_80070308(gobj);
+#endif
     ftCo_800C884C(gobj);
 
     Fighter_80068E64(gobj);
--- a/src/sysdolphin/baselib/class.c
+++ b/src/sysdolphin/baselib/class.c
@@ -7,6 +7,7 @@
 #include "object.h" // IWYU pragma: keep
 
 #include <__mem.h>
+#include <stddef.h>
 #include <string.h>
 #include <dolphin/os.h>
 
@@ -43,7 +44,7 @@
         HSD_ASSERT(94, class_info->head.obj_size >= parent_info->head.obj_size);
         HSD_ASSERT(95, class_info->head.info_size >= parent_info->head.info_size);
         memcpy(&class_info->alloc, &parent_info->alloc,
-               parent_info->head.info_size - 0x28);
+               parent_info->head.info_size - offsetof(HSD_ClassInfo, alloc));
         class_info->head.next = parent_info->head.child;
         parent_info->head.child = class_info;
     }
@@ -84,11 +85,12 @@
 
             for (new_nb = 32; idx >= new_nb; new_nb *= 2) {
             }
-            memory_list = (HSD_MemoryEntry**) HSD_MemAlloc(new_nb * 4);
+            memory_list = (HSD_MemoryEntry**) HSD_MemAlloc(
+                new_nb * sizeof(*memory_list));
             if (memory_list == NULL) {
                 return NULL;
             }
-            memset(memory_list, 0, new_nb * 4);
+            memset(memory_list, 0, new_nb * sizeof(*memory_list));
             nb_memory_list = new_nb;
         } else { // Resizes the array
             HSD_MemoryEntry** old_list;
@@ -100,19 +102,20 @@
                 new_nb *= 2;
             }
 
-            new_list = HSD_MemAlloc(4 * new_nb);
+            new_list = HSD_MemAlloc(sizeof(*new_list) * new_nb);
             if (new_list == NULL) {
                 return NULL;
             }
 
-            memcpy(new_list, memory_list, 4 * nb_memory_list);
+            memcpy(new_list, memory_list,
+                   sizeof(*new_list) * nb_memory_list);
             memset(&new_list[nb_memory_list], 0,
-                   4 * (new_nb -
-                        nb_memory_list)); // You start *after* existing ptrs
+                   sizeof(*new_list) *
+                       (new_nb - nb_memory_list)); // You start *after* existing ptrs
                                           // and make sure memory is zero'd
 
             old_list = memory_list;
-            old_nb = OSRoundDown32B(nb_memory_list * 4);
+            old_nb = OSRoundDown32B(nb_memory_list * sizeof(*memory_list));
             memory_list = new_list;
             nb_memory_list = new_nb;
 
--- a/src/melee/ft/ftparts.c
+++ b/src/melee/ft/ftparts.c
@@ -509,6 +509,19 @@
 void ftParts_8007487C(FtPartsDesc* desc, FtPartsVis* vis, u32 costume_id,
                       DObjList* arg3, DObjList* arg4)
 {
+#ifdef MSL_CORE_HOSTED
+    int i;
+
+    // The visibility tables below address DObjs and only control rendered
+    // costume/model variants. Preserve the source-owned model bookkeeping;
+    // gameplay joints were already published by ftParts_SetupParts.
+    // refs/melee/src/melee/ft/ftparts.c::ftParts_8007487C
+    vis->model_num = desc->model_num;
+    for (i = 0; i < 5; i++) {
+        vis->xC[i] = NULL;
+        vis->cleared[i] = false;
+    }
+#else
     void*(*vis_table)[4];
     PAD_STACK(0x8);
 
@@ -537,6 +550,7 @@
     ftParts_80074D7C(vis, 2, arg4);
     ftParts_80074D7C(vis, 3, arg3);
     ftParts_80074D7C(vis, 4, arg3);
+#endif
 }
 
 void ftParts_800749CC(Fighter_GObj* gobj)
--- a/src/melee/lb/lbarchive.c
+++ b/src/melee/lb/lbarchive.c
@@ -54,7 +54,7 @@
 {
     HSD_Archive* archive;
     void* data;
-    size_t length;
+    u32 length;
 
     data = lbHeap_80015BD0(0, OSRoundUp32B(lbFile_800163D8(filename)));
     archive = lbHeap_80015BD0(0, sizeof(HSD_Archive));
@@ -150,7 +150,7 @@
 bool lbArchive_80016F80(HSD_Archive** archive, const char* filename)
 {
     void* data;
-    size_t length;
+    u32 length;
     HSD_Archive* var_r3;
     bool result;
     u8 _[8];
@@ -191,7 +191,7 @@
         // Inlined lbArchive_LoadArchive
         {
             void* data;
-            size_t length;
+            u32 length;
             u32 pad;
             u32 pad2;
             data = lbHeap_80015BD0(0, OSRoundUp32B(lbFile_800163D8(filename)));
@@ -232,7 +232,7 @@
         // Inlined lbArchive_LoadArchive
         {
             void* data;
-            size_t length;
+            u32 length;
             u32 pad;
             u32 pad2;
            data = lbHeap_80015BD0(0, OSRoundUp32B(lbFile_800163D8(filename)));
--- a/src/melee/lb/lbspdisplay.c
+++ b/src/melee/lb/lbspdisplay.c
@@ -120,8 +120,8 @@
 
 void lb_8000FCDC(void)
 {
-    lb_804D63A0 = HSD_MemAlloc(0xBE00);
-    lb_804D63A8 = HSD_MemAlloc(0x1C0);
+    lb_804D63A0 = HSD_MemAlloc(sizeof(*lb_804D63A0));
+    lb_804D63A8 = HSD_MemAlloc(sizeof(*lb_804D63A8));
     lb_8000FA94();
 }
 
--- a/src/sysdolphin/baselib/archive.c
+++ b/src/sysdolphin/baselib/archive.c
@@ -3,6 +3,10 @@
 #include <string.h>
 #include <dolphin/os.h>
 
+#ifdef MSL_CORE_NATIVE
+#include "platform/native_dat.h"
+#endif
+
 static inline void HSD_ArchiveLocate(HSD_Archive* archive)
 {
     u32 i;
@@ -16,6 +20,9 @@
 
 s32 HSD_ArchiveParse(HSD_Archive* archive, u8* src, size_t file_size)
 {
+#ifdef MSL_CORE_NATIVE
+    return msl_native_archive_parse(archive, src, file_size);
+#else
     u32 offset;
 
     if (archive == NULL) {
@@ -62,10 +69,14 @@
     HSD_ArchiveLocate(archive);
 
     return 0;
+#endif
 }
 
 void* HSD_ArchiveGetPublicAddress(HSD_Archive* archive, const char* symbols)
 {
+#ifdef MSL_CORE_NATIVE
+    return msl_native_archive_get_public(archive, symbols);
+#else
     u32 i;
 
     for (i = 0; i < archive->header.nb_public; i++) {
@@ -79,20 +90,28 @@
     }
 
     return NULL;
+#endif
 }
 
 char* HSD_ArchiveGetExtern(HSD_Archive* archive, int offset)
 {
+#ifdef MSL_CORE_NATIVE
+    return msl_native_archive_get_extern(archive, offset);
+#else
     if (offset < 0 || archive->header.nb_extern <= (unsigned) offset) {
         return NULL;
     }
 
     return archive->symbols + archive->extern_info[offset].symbol;
+#endif
 }
 
 void HSD_ArchiveLocateExtern(HSD_Archive* archive, const char* symbols,
                              void* addr)
 {
+#ifdef MSL_CORE_NATIVE
+    msl_native_archive_locate_extern(archive, symbols, addr);
+#else
     uintptr_t next;
     uintptr_t offset = -1;
     u32 i;
@@ -116,4 +135,5 @@
         *(u32*) ((uintptr_t) archive->data + offset) = (uintptr_t) addr;
         offset = next;
     }
+#endif
 }
