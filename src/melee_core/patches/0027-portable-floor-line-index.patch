--- a/src/melee/mp/mplib.c
+++ b/src/melee/mp/mplib.c
@@ -1648,9 +1648,12 @@
                 continue;
             }
 
-            if (line_id_skip ==
-                (line_offset = (s32) line_r26 - (s32) groundCollLine) / 8)
-            {
+            // On retail sizeof(CollLine) is 8, so the original byte-distance
+            // calculation is also the array index. Native pointers widen the
+            // structure; typed subtraction preserves the source-owned index.
+            // refs/melee/src/melee/mp/mplib.c::mpCheckFloor
+            line_offset = line_r26 - groundCollLine;
+            if (line_id_skip == line_offset) {
                 continue;
             }
 
@@ -1661,7 +1664,7 @@
                 continue;
             }
 
-            mpLib_8004ED5C(line_offset / 8, &x0_sp48, &y0_sp44, &x1_sp40,
+            mpLib_8004ED5C(line_offset, &x0_sp48, &y0_sp44, &x1_sp40,
                            &y1_sp3C);
             y0_sp44 += y_offset;
             y1_sp3C += y_offset;
