--- a/src/melee/gr/types.h
+++ b/src/melee/gr/types.h
@@ -1860,10 +1860,16 @@ struct UnkStageDat_x8_t {
     /* +28 */ UNK_T x28;
     /* +2C */ s16* x2C;
     /* +30 */ int x30;
 };
 
+struct UnkStageDatJointEntry {
+    HSD_Joint* joint;
+    s16* pairs;
+    s32 pair_count;
+};
+
 struct UnkStageDat {
-    void* unk0;
+    struct UnkStageDatJointEntry* unk0;
     s32 unk4;
 
     struct UnkStageDat_x8_t* unk8; // Suspect this may not be a consistent type
--- a/src/melee/gr/ground.c
+++ b/src/melee/gr/ground.c
@@ -1896,12 +1896,8 @@ void Ground_801C34AC(s32 map_id, HSD_JObj* root, struct HSD_Joint* joint)
     StageInfo* stageinfo;
     UnkStageDat* stage_dat;
     UnkArchiveStruct* archive;
     int entry_count;
-    struct {
-        void* joint;
-        s16* pairs;
-        s32 pair_count;
-    }* entry;
+    struct UnkStageDatJointEntry* entry;
     int count;
     s16* pair;
     int prev_index;
@@ -1986,11 +1982,8 @@ void Ground_801C36F4(int map_id, HSD_JObj* root, UNK_T joint)
     HSD_JObj* jobj;
     UnkStageDat* stage_dat;
     UnkArchiveStruct* archive;
     int entry_count;
-    struct {
-        void* joint;
-        u8 x4_pad[0x8];
-    }* entry;
+    struct UnkStageDatJointEntry* entry;
     int i;
     u32 unused[4];
 
@@ -3248,13 +3241,7 @@ static inline s32 randi(s32 max)
 
 int Ground_801C5940(void)
 {
-    struct {
-        u8 x0_pad[0x4];
-        struct {
-            s16 a, b;
-        }* unk4;
-        s32 unk8;
-    }* phi_r8;
+    struct UnkStageDatJointEntry* phi_r8;
     int i, j, out_idx;
     UnkArchiveStruct* archive;
     const size_t vals_count = 32;
@@ -3267,9 +3254,9 @@ int Ground_801C5940(void)
     }
     phi_r8 = archive->unk4->unk0;
     for (i = 0; i < archive->unk4->unk4; i++, phi_r8++) {
-        int max = phi_r8->unk8;
+        int max = phi_r8->pair_count;
         for (j = 0; j < max; j++) {
-            int val = phi_r8->unk4[j].b;
+            int val = phi_r8->pairs[j * 2 + 1];
             if (val >= 220 && val < 252 && (unsigned) out_idx < vals_count) {
                 vals[out_idx] = val;
                 out_idx++;
