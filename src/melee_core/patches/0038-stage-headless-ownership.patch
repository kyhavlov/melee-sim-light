--- a/src/melee/gr/grbattle.c
+++ b/src/melee/gr/grbattle.c
@@ -169,7 +169,9 @@
 
 static void grBattle_OnStart(void)
 {
+#ifndef MSL_CORE_HOSTED
     grZakoGenerator_801CAE04(NULL);
+#endif
 }
 
 static bool grBattle_80219D7C(void)
@@ -188,7 +190,9 @@
         Ground* gp = GET_GROUND(gobj);
         gp->x8_callback = NULL;
         gp->xC_callback = NULL;
+#ifndef MSL_CORE_HOSTED
         GObj_SetupGXLink(gobj, grDisplay_801C5DB0, 3, 0);
+#endif
 
         if (callbacks->callback3 != NULL) {
             gp->x1C_callback = callbacks->callback3;
@@ -289,7 +293,9 @@
     HSD_JObj* jobj = GET_JOBJ(gobj);
 
     grAnime_801C8138(gobj, gp->map_id, 0);
+#ifndef MSL_CORE_HOSTED
     grMaterial_801C94D8(jobj);
+#endif
     gp->x11_flags.b012 = 2;
 }
 
@@ -307,7 +313,9 @@
     Ground* gp = GET_GROUND(gobj);
     HSD_JObj* jobj = GET_JOBJ(gobj);
     grAnime_801C8138(gobj, gp->map_id, 0);
+#ifndef MSL_CORE_HOSTED
     grMaterial_801C94D8(jobj);
+#endif
     gp->x11_flags.b012 = 2;
 }
 
@@ -325,7 +333,9 @@
     Ground* gp = GET_GROUND(gobj);
     HSD_JObj* jobj = GET_JOBJ(gobj);
     grAnime_801C8138(gobj, gp->map_id, 0);
+#ifndef MSL_CORE_HOSTED
     grMaterial_801C94D8(jobj);
+#endif
     gp->x11_flags.b012 = 2;
 }
 
@@ -411,11 +421,15 @@
 
             bg_gobj = Ground_801C2BA4(gp->u.battle.prev_bg);
             HSD_ASSERT(535, bg_gobj);
+#ifndef MSL_CORE_HOSTED
             grMaterial_801C9604(bg_gobj, grNBa_804D6ACC->unk4, 0);
+#endif
 
             bg_gobj = grBattle_80219D84(gp->u.battle.curr_bg);
             HSD_ASSERT(539, bg_gobj);
+#ifndef MSL_CORE_HOSTED
             grMaterial_801C9604(bg_gobj, grNBa_804D6ACC->unk0, 0);
+#endif
 
             gp->u.battle.bg_state = BG_Done;
         }
--- a/src/melee/gr/ground.c
+++ b/src/melee/gr/ground.c
@@ -106,6 +106,7 @@
     0,
 };
 
+#ifndef MSL_CORE_HOSTED
 extern StageData Ground_803DFEA8, grTe_803E5764, grCs_803E11A4, grRc_803E4ECC,
     grKg_803E1800, grGd_803E52E0, grGb_803E3F6C, grSh_803E5130, grZe_803E1B2C,
     grKr_803E4D0C, grSt_803E274C, grYt_803E51CC, grIz_803E0E5C, grGr_803E76D0,
@@ -163,6 +164,11 @@
     &grTe_803E5764,       &grTe_803E5764,       &grTe_803E5764,
     &grTe_803E5764,       &grTe_803E5764,       &grTe_803E5764,
 };
+#define MSL_CORE_STAGE_DATA(stage_id) Ground_803DFEDC[stage_id]
+#else
+extern StageData* msl_core_selected_stage_data(void);
+#define MSL_CORE_STAGE_DATA(stage_id) msl_core_selected_stage_data()
+#endif
 
 #define _BUFFER_SZ (64)
 
@@ -415,11 +421,11 @@
 
 void Ground_801C06B8(InternalStageId arg0)
 {
-    if (Ground_803DFEDC[arg0] == NULL) {
+    if (MSL_CORE_STAGE_DATA(arg0) == NULL) {
         return;
     }
-    if (Ground_803DFEDC[arg0]->data1 != NULL) {
-        lbDvd_800178E8(4, Ground_803DFEDC[arg0]->data1, 4, 4, 0, 1, 7, 16, 0);
+    if (MSL_CORE_STAGE_DATA(arg0)->data1 != NULL) {
+        lbDvd_800178E8(4, MSL_CORE_STAGE_DATA(arg0)->data1, 4, 4, 0, 1, 7, 16, 0);
     }
     switch (arg0) {
     case IZUMI:
@@ -439,7 +445,7 @@
     s32 arg3;
     Ground_801BFFB0();
     stage_info.internal_stage_id = pair->stage_id;
-    stage = Ground_803DFEDC[pair->stage_id];
+    stage = MSL_CORE_STAGE_DATA(pair->stage_id);
     arg3 = (pair->list_idx == 0x55) ? 0 : 1;
     grDatFiles_801C6038(stage->data1, 0, arg3);
     Ground_801C28CC(&stage_info.xA0, pair->list_idx);
@@ -450,7 +456,7 @@
 
 void Ground_801C0800(StructPairWithStageID* pair)
 {
-    StageData* stage_data = Ground_803DFEDC[pair->stage_id];
+    StageData* stage_data = MSL_CORE_STAGE_DATA(pair->stage_id);
     Ground_801C38D0(stage_info.param->x8, stage_info.param->x14,
                     stage_info.param->x1C, stage_info.param->x18);
     Ground_801C38EC(stage_info.param->x10, stage_info.param->xC);
@@ -670,7 +676,7 @@
 
 void Ground_801C0F78(StructPairWithStageID* pair)
 {
-    Ground_803DFEDC[pair->stage_id]->OnLoad();
+    MSL_CORE_STAGE_DATA(pair->stage_id)->OnLoad();
 }
 
 void Ground_801C0FB8(StructPairWithStageID* pair)
@@ -681,7 +687,7 @@
         void (*unk8)(s32);
     }* cur;
     void* next;
-    Ground_803DFEDC[pair->stage_id]->OnStart();
+    MSL_CORE_STAGE_DATA(pair->stage_id)->OnStart();
     for (cur = stage_info.x6A4; cur != NULL; cur = next) {
         next = cur->unk0;
         cur->unk8(cur->unk4);
@@ -694,7 +700,7 @@
 
 void Ground_DemoInit(StructPairWithStageID* pair, s32 arg1)
 {
-    Ground_803DFEDC[pair->stage_id]->OnDemoInit(arg1);
+    MSL_CORE_STAGE_DATA(pair->stage_id)->OnDemoInit(arg1);
 }
 
 void Ground_801C10B8(HSD_GObj* arg0, HSD_GObjEvent arg1)
@@ -817,7 +823,6 @@
 #ifdef MSL_CORE_HOSTED
     extern Ground* msl_core_stage_ground_alloc(void);
     extern void msl_core_stage_ground_free(void* ground);
-    extern StageCallbacks* msl_core_stage_callbacks(void);
 #endif
 
     gobj = GObj_Create(HSD_GOBJ_CLASS_STAGE, 5, 0);
@@ -860,7 +865,9 @@
         }
     }
 
+#ifndef MSL_CORE_HOSTED
     grMaterial_801C95C4(gobj);
+#endif
     archive = grDatFiles_801C6324();
     HSD_ASSERT(1358, archive);
 
@@ -881,15 +888,11 @@
             OSReport("%s:%d: couldn t get jobj\n", __FILE__, 0x55D);
             return NULL;
         }
-#ifdef MSL_CORE_HOSTED
-        if (msl_core_stage_callbacks()[map_id].flags_b2 == 1 &&
-            archive->unk4->unk8[map_id].x10 != NULL)
-#else
-        if (Ground_803DFEDC[stageinfo->internal_stage_id]
+#ifndef MSL_CORE_HOSTED
+        if (MSL_CORE_STAGE_DATA(stageinfo->internal_stage_id)
                     ->callbacks[map_id]
                     .flags_b2 == 1 &&
             archive->unk4->unk8[map_id].x10 != NULL)
-#endif
         {
             HSD_GObj* temp_r23_2 = GObj_Create(17, 19, 0);
             temp_r27 = lb_80013B14(archive->unk4->unk8[map_id].x10);
@@ -900,6 +903,7 @@
             temp_r3->x18 = temp_r23_2;
             Ground_801C2BD4(temp_r27);
         }
+#endif
         HSD_GObjObject_80390A70(gobj, HSD_GObj_804D7849, new_var);
         phi_r24 = archive->unk4->unk8[map_id].x30;
         phi_r23 = archive->unk4->unk8[map_id].x2C;
@@ -990,7 +994,9 @@
     HSD_JObj* jobj = gobj->hsd_obj;
     Ground* gp = gobj->user_data;
     HSD_JObjAnimAll(jobj);
+#ifndef MSL_CORE_HOSTED
     grMaterial_801C9698(gobj);
+#endif
     mpColl_804D64AC += 1;
     if (gp->x8_callback != NULL) {
         gp->x8_callback(gobj);
@@ -1091,7 +1097,7 @@
     grDatFiles_801C6324();
     archive = grDatFiles_801C6324();
     stage_id = stage_info.internal_stage_id;
-    temp_r29 = Ground_803DFEDC[stage_id]->callbacks;
+    temp_r29 = MSL_CORE_STAGE_DATA(stage_id)->callbacks;
     temp_r30 = archive->unk4->unkC;
     grDatFiles_801C6324();
     for (i = 0; i < temp_r30; i++) {
@@ -1669,8 +1675,8 @@
             result = true;
         }
     }
-    max = Ground_803DFEDC[stage_info.internal_stage_id]->x30;
-    cur = Ground_803DFEDC[stage_info.internal_stage_id]->x2C;
+    max = MSL_CORE_STAGE_DATA(stage_info.internal_stage_id)->x30;
+    cur = MSL_CORE_STAGE_DATA(stage_info.internal_stage_id)->x2C;
     for (i = 0; i < max; i++, cur++) {
         if (cur->y == arg1) {
             mpLib_800552B0(cur->x, jobj, cur->z);
@@ -1708,7 +1714,7 @@
 
         temp_r3 = mpGetGroundCollJoint();
         Ground_804D6954++;
-        stagedata = Ground_803DFEDC[stage_info.internal_stage_id];
+        stagedata = MSL_CORE_STAGE_DATA(stage_info.internal_stage_id);
         count = stagedata->x30;
         vec = stagedata->x2C;
 
@@ -1754,8 +1760,8 @@
         /// @todo @c cur cannot be swapped below @c max, hinting at a missing
         /// @c inline function.
         S16Vec3* cur;
-        int max = Ground_803DFEDC[stage_info.internal_stage_id]->x30;
-        cur = Ground_803DFEDC[stage_info.internal_stage_id]->x2C;
+        int max = MSL_CORE_STAGE_DATA(stage_info.internal_stage_id)->x30;
+        cur = MSL_CORE_STAGE_DATA(stage_info.internal_stage_id)->x2C;
         {
             int i;
             for (i = 0; i < max; i++, cur++) {
@@ -1814,8 +1820,8 @@
     int i;
     mpGetGroundCollJoint();
     /// @todo Might be an @c inline starting here.
-    max = Ground_803DFEDC[stage_info.internal_stage_id]->x30;
-    cur = Ground_803DFEDC[stage_info.internal_stage_id]->x2C;
+    max = MSL_CORE_STAGE_DATA(stage_info.internal_stage_id)->x30;
+    cur = MSL_CORE_STAGE_DATA(stage_info.internal_stage_id)->x2C;
     result = -1;
     for (i = 0; i < max; cur++, i++) {
         if (cur->y == arg0 && cur->z == arg1) {
@@ -1849,8 +1855,8 @@
     int i;
     mpGetGroundCollJoint();
     /// @todo Might be an @c inline starting here.
-    max = Ground_803DFEDC[stage_info.internal_stage_id]->x30;
-    cur = Ground_803DFEDC[stage_info.internal_stage_id]->x2C;
+    max = MSL_CORE_STAGE_DATA(stage_info.internal_stage_id)->x30;
+    cur = MSL_CORE_STAGE_DATA(stage_info.internal_stage_id)->x2C;
     result = -1;
     for (i = 0; i < max; cur++, i++) {
         if (cur->y == arg0 && cur->x == arg1) {
@@ -2604,7 +2610,7 @@
     int temp_r28;
 
     temp_r28 = grDatFiles_801C6324()->unk4->unkC;
-    var_r26 = Ground_803DFEDC[stage_info.internal_stage_id]->callbacks;
+    var_r26 = MSL_CORE_STAGE_DATA(stage_info.internal_stage_id)->callbacks;
     grDatFiles_801C6324();
 
     for (i = 0; i < temp_r28; i++) {
@@ -3321,7 +3327,7 @@
 
 u32 Ground_801C5AD0(s32 i)
 {
-    return Ground_803DFEDC[i]->flags2;
+    return MSL_CORE_STAGE_DATA(i)->flags2;
 }
 
 void Ground_801C5AEC(Vec3* v, Vec3* arg1, Vec3* arg2, Vec3* arg3)
--- a/src/melee/gr/grpstadium.c
+++ b/src/melee/gr/grpstadium.c
@@ -187,7 +187,9 @@
 
 void grStadium_OnStart(void)
 {
+#ifndef MSL_CORE_HOSTED
     grZakoGenerator_801CAE04(NULL);
+#endif
 }
 
 bool grStadium_801D10F0(void)
@@ -211,7 +213,9 @@
         Ground* gp = GET_GROUND(gobj);
         gp->x8_callback = NULL;
         gp->xC_callback = NULL;
+#ifndef MSL_CORE_HOSTED
         GObj_SetupGXLink(gobj, grDisplay_801C5DB0, 3, 0);
+#endif
         if (cb->callback3 != NULL) {
             gp->x1C_callback = cb->callback3;
         }
