[project]
name = "melee-sim-light"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "peppi-py",
]

[dependency-groups]
dev = [
  "pytest",
  "pytest-xdist",
  "setuptools",
]
