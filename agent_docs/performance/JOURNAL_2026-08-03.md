# Performance campaign journal — 2026-08-03

This journal preserves the complete experiment record between checkpoint `5fac340b` and the
first retained >=5% throughput checkpoint. Labels describe historical state; only the final
retained checkpoint remains in the runtime.


## Contract and frozen control

- Branch: `perf/decomp-throughput`; exact control commit `5fac340b`.
- Frozen control binary: `build/melee_core/perf-control-5fac340b/replay-bench`, SHA-256
  `a461e1301379b32f00ea5e8eb56e73d3c60c31f1be387ee0749ee98821fec0ff`.
- Resident-256 control: 42,429.7 median cycles/frame and 101,155 FPS, digest
  `bdff41cf74a54850`.
- Resident-512 control: 40,783.0 median cycles/frame and 105,239 FPS, digest
  `ee9d93c545aa3ef9`.
- First checkpoint requires at most 40,409.2 / 38,841.0 controlled median cycles/frame at
  resident 256 / 512, corresponding to at least +5% throughput at both sizes.
- Final target is at least 150,000 median FPS at both sizes, approximately 28,600 cycles/frame on
  this host.

## Current packet

- **Scope:** delete repeated exact Euler-to-quaternion conversion of immutable authored fighter
  joints in `lb_8000C868`. Compiler-setting experiments are excluded.
- **Final owner:** initialized immutable fighter data owns the authored `HSD_Joint.rotation`; any
  retained derived quaternion must have the same process-wide lifetime and remain read-only during
  gameplay.
- **Canonical state:** unchanged authored Euler rotation and live JObj pose. The derived quaternion
  is an exact memoized product, not a second mutable pose representation.
- **Consumers:** native `ftAnim_80070010` / `ftAnim_80070108` guard and directional-pose blends.
- **Displaced work/state:** repeated `EulerToQuat` calls for the authored input only. Live-pose
  conversion, sign selection, slerp, SRT publication, and PPC source behavior remain unchanged.
- **Deletion boundary:** first screen the potential with a fixed allocation-free diagnostic cache.
  Retain no lazy process-global mutable state: a positive result must move to initialization-owned
  immutable data; a negative result removes the cache completely.
- **Acceptance:** exact benchmark digests and focused guard validation, no gameplay allocation or
  unexplained memory growth, and retained code only when whole-runtime controlled measurements
  support it.

## Log

- 2026-08-03 — `baseline`: froze the exact committed control binary and confirmed its SHA against
  `agent_docs/performance/BASELINE.md`. Next: profile the corrected balanced workload and select
  the first code owner with material deletable cost.
- 2026-08-03 — `open`: delete the redundant initialized-byte validation traversal from the normal
  fully initialized batch step. Final owner is the private `MslCoreBatch`; canonical state remains
  the per-match initialized byte, summarized by one batch-level initialized count maintained at
  the three existing zero-to-one publication sites (reset, copy, and restore). The step API is the
  only consumer. Partially initialized batches retain the original complete preflight before any
  Match mutates, preserving error atomicity; fully initialized batches can prove the same condition
  in O(1). No Match/savestate state, allocation, public API, scheduler order, or fallback is added.
  Retain only if exact digests and alternating whole-runtime comparisons beat the current wide-pose
  candidate at both resident sizes.
- 2026-08-03 — `rejected`: the initialized-count fast path preserves both exact digests but loses
  every completed reversal. Resident-256 old/new pairs are `39,466.2/39,885.5`,
  `39,616.6/40,005.4`, and `39,950.7/40,248.2` cycles/frame. The first resident-512 pair is
  `37,953.5/38,340.3`, and the next candidate is slower again at `39,030.9`. The earlier 0.55%
  attribution was incorrect: subsystem-profile's `prepare` row times
  `msl_core_match_step_prepare`, not this batch preflight. The byte scan is negligible and may
  provide favorable loop/layout effects; the summary field and all maintenance are removed.
- 2026-08-03 — `open`: compact each retained wide blend to the joints that actually require
  quaternion work. Final owner, canonical JObj SRT, and consumers are unchanged. The existing
  near-equal Euler exits publish immediately; remaining joints keep source order in compact
  quaternion/output arrays, deleting the activity byte array and its second-pass branch. No pose
  state, allocation, approximation, callback ordering, or fallback is added. Retain only if exact
  digests and alternating comparisons beat the current wide-pose candidate at both sizes.
- 2026-08-03 — `rejected`: compacting active blends preserves both exact digests but loses the
  resident-512 reversals. The first two old/new pairs are `37,979.0/38,475.9` (+1.31% cycles) and
  `38,302.9/38,598.2` (+0.77%). The extra compact output-pointer stream costs more than the
  predictable activity branch it removes. Restore the indexed quaternion arrays and activity
  bytes; no compact-blend code remains.
- 2026-08-03 — `profile`: disposable inner timing bounds the entire retained wide-blend tree to
  25.48M cycles, 1.63% of the 32,768-frame resident-512 diagnostic contract: SRT/quaternion
  preparation is 8.25M (0.53%), live Euler conversion is 4.00M (0.26%), and sign selection plus
  slerp/publication is 13.24M (0.84%). Further blend plumbing cannot materially advance 150k and
  is closed after the measured layout regressions. All timing buckets, build hooks, and includes
  are removed.
- 2026-08-03 — `open`: specialize the dominant compact-pose interpreter's already-canonical
  direct integer Figa case before entering its decoder/loop/end state machine. Final owner remains
  `MslFighterPoseJoint` plus the immutable program sample; canonical outputs remain JObj SRT and
  the existing frame/flag/callback counters. A node is admitted only when its exact next frame is
  an in-range direct table sample strictly before loop/end handling; it performs the same frame
  update, rewind-bit clear, sample publication, decoder-table ownership update, and active count,
  then returns. All boundary, fractional, descriptor, and decoder cases retain the singular source
  path. No state, allocation, approximate math, fallback representation, or callback reorder is
  added.
- 2026-08-03 — `rejected`: the direct-sample early path preserves both digests but the first two
  resident-512 old/new comparisons regress `38,372.1 -> 38,451.2` and `38,372.1 -> 38,522.8`
  cycles/frame. Disassembly explains the loss: the second call site makes GCC outline the 0x698-byte
  sample publisher; total interpreter/publisher text grows from 0x120b to 0x14fc and every hot table
  node gains a call. Remove the duplicated admission completely. Any later interpreter split must
  keep the direct publisher singular and move only genuinely cold decoder ownership.
- 2026-08-03 — `open`: separate only the non-table decoder transition from `interpret_joint`.
  Final state and ownership are unchanged: the same compact node, lazy tracks, and decoder execute
  in the same order whenever direct table publication declines. The direct publisher retains one
  call site so GCC can keep it in the hot interpreter; the fractional/descriptor/materialization
  block becomes one ordinary external leaf, paid only on that existing branch. This adds no
  admission state, duplicate evaluator, optimization attribute, allocation, approximation, or
  fallback. Retain only if disassembly confirms a smaller hot owner and both resident sizes win.
- 2026-08-03 — `rejected`: the split reaches the intended compiler shape—the singular direct
  publisher stays in the interpreter and its hot text falls from 0x120b to 0xa05 bytes—but the
  decoder leaf call cancels that benefit. Resident-512 old/new pairs are
  `38,506.0/38,432.0`, `37,984.9/38,216.5`, and `38,151.2/38,234.7` cycles/frame: one 0.19% win,
  then 0.61% and 0.22% losses. Restore the single interpreter and internal decoder block; no
  external leaf or renamed symbol remains.
- 2026-08-03 — `open`: complete the Dream Land object-1 headless visual-response deletion without
  repeating the earlier incomplete callback removal. Final gameplay owner remains object 7's
  Whispy controller; canonical gameplay state includes object 1's `xD0` response handshake, which
  must still clear from one to zero in its source scheduler slot. Native/Wasm object 1 then omits
  only the `grAnime_801C8138` presentation request selected from `xC8/xCC/xD4`; PPC retains the
  source call. Consumers are unchanged Whispy state and public observation/terminal output.
  Displaced state is only presentation AObj/FObj animation on a non-collision background JObj;
  no callback, RNG draw, stage-line state, gameplay field, allocation, or schedule entry is
  removed. Retain only with both exact production digests and the complete Dream Land/full gates.
- 2026-08-03 — `profile`: the balanced resident-512 subsystem build attributes 14.96% to pose
  animation, 13.04% to stage-collision callbacks, 6.71% to `Fighter_8006D9AC`, 4.79% to action
  input, 3.95% to camera, and 2.56% to observation. The top scalar alternatives are already
  closed in the consolidated history. In particular, do not repeat the Dream Land visual callback
  deletion: it changed the current 366-case resident-512 digest despite passing the old domain.
- 2026-08-03 — `open`: test direct hosted dynamics re-anchoring from the solver's canonical
  `unk_2C`, deleting the post-solve full-matrix traversal. The current full rebuild exists solely
  to make the same solved origin available to `ftCo_8009CB40`; exact digest and validation decide
  whether the two publications are truly equivalent over the complete domain.
- 2026-08-03 — `rejected`: the solver anchor and render-matrix translation are not interchangeable.
  Resident-256 changed digest `a00e2d90ed375c84 -> e59393ebfc38505c`; the locked Mario/Fox
  replay `33692` forks at frame 4,428 even when the matrix rebuild remains present but the
  transition retains `unk_2C`. Retail's dynamics and render paths have distinct exact float
  operation streams. The substitution is removed. Next: measure solver versus rebuild cost and
  consider only an integrated evaluator that preserves both streams while sharing traversal/trig.
- 2026-08-03 — `closed`: separate attribution assigns 199.69M cycles to the demanded dynamics
  solver and only 18.42M cycles (0.55% of the instrumented contract) to the exact render-matrix
  refresh. An integrated dual-operation evaluator cannot justify its complexity or materially
  advance the target. All source substitutions and timing instrumentation are removed.
- 2026-08-03 — `open`: screen exact reuse of `lb_8000C868`'s immutable authored quaternion. The
  prior fused guard evaluator still performed both required conversions and regressed; this cut
  leaves the live conversion and both ordered blends intact. A fixed lazy cache is diagnostic only
  and cannot be retained as the final owner.
- 2026-08-03 — `rejected form`: a 4,096-entry direct-mapped cache hit 371,839 / 390,078
  initialization-plus-gameplay calls (95.3%) but made all three resident-512 pairs 0.84--1.29%
  slower. Hashing and the 96 KiB mutable table enlarged an already register-heavy blend enough to
  exceed the conversion it removed. The cache and counters are removed completely; this does not
  reject initialization-owned direct consumption.
- 2026-08-03 — `retained candidate`: native DAT translation now appends the exact authored
  quaternion to each immutable `HSD_Joint`; `lb_8000C868` consumes it with one direct 16-byte load.
  No hash, gameplay mutation, Match state, or runtime allocation remains. Three 131,072-frame
  alternating screens preserve digests `a00e2d90ed375c84` / `9c1f37ab084f8ef2`. Resident-256
  medians improve `42,045.6 -> 41,511.9` cycles/frame (-1.27%, `102,078 -> 103,391` FPS);
  resident-512 medians improve `40,560.6 -> 40,508.9` (-0.13%, `105,816 -> 105,951` FPS).
  The 9,464 translated joints add 302,848 aligned bytes to process-wide sealed native DAT data;
  Match memory and the 608,864-byte / 843-allocation gameplay lock are unchanged. Native smoke and
  locked replay `33692` (10,645 / 10,645 exact frames) pass. Retain uncommitted as a small
  cumulative gain; it is not a checkpoint.
- 2026-08-03 — `rejected`: directly inlining the unchanged exact SRT-concat primitive into the
  compact ECB publisher removes every per-matrix wrapper call and preserves digest
  `75046e348333b63b`, but loses both resident-512 reversals. Controls measured 40,187.7 and
  40,115.9 cycles/frame; candidates measured 40,575.9 and 40,375.9 (+0.97% / +0.65%). The larger
  register-heavy publisher outweighs its call deletion. The private header and call-site change
  are removed completely.
- 2026-08-03 — `rejected`: replacing the AVX-512 trig kernel's two tiny quadrant-table gathers
  with mask-produced positive zero/signed one preserves both digests but loses all four 131,072-
  frame reversals. Resident-512 controls/candidates are 39,509.5/39,699.5 and
  39,630.2/39,664.7 cycles/frame; resident-256 candidates/controls are 41,323.8/41,062.4 and
  41,373.4/41,221.0. The table stays hot and its gathers cost less than the replacement mask work.
  The trig source is restored exactly.
- 2026-08-03 — `rejected`: a bounded resident-512 census finds exact unit local scale on
  9,590,391 / 9,765,729 direct ECB matrices (98.2%) and exact unit accumulated parent scale on
  all 9,765,729. A live-value-admitted exact unit primitive deletes the corresponding correction
  and local-scale math, but controlled resident-512 medians are neutral and resident-256 medians
  regress about 0.5%. Same-instrumentation stage-collision cycles also rise
  `426,858,119 -> 427,810,483`; this is not a displaced gain. Admission/code footprint costs more
  than the unit multiplications. The primitive, declaration, and temporary counters are removed.
- 2026-08-03 — `open`: evaluate the four independent half-angles in the hosted dynamics
  quaternion update through the existing exact native `msl_sincosf_many` owner. Final owner is
  `msl_dynamics_rotate_euler`; canonical state remains the live JObj Euler rotation, and the
  dynamics solver remains its only consumer. The candidate displaces eight scalar `sinf`/`cosf`
  polynomial evaluations with one paired evaluation of the identical four inputs and outputs.
  There is no new state, allocation, approximation, phase boundary, or fallback; PPC keeps the
  source path. Retain only if both benchmark digests and controlled whole-runtime measurements
  support it.
- 2026-08-03 — `rejected`: the four-lane exact SIMD trig candidate preserves both 131,072-frame
  digests, but its masked AVX-512 setup costs more than the eight scalar evaluations. All three
  resident-512 candidates lose to their adjacent quaternion-only controls (`40,248.0`, `40,349.0`,
  `40,289.3` candidate cycles/frame versus `39,463.3`, `39,885.1`, `39,677.5` controls).
  Resident-256 is likewise slower overall (`41,793.7`, `42,042.4`, `42,334.2` candidates versus
  `41,414.7`, `41,484.3`, `42,314.6` controls). The SIMD path and its stack arrays are removed;
  no dynamics code remains changed.
- 2026-08-03 — `open`: reassess the proven shared-reduction opportunity without SIMD or temporary
  arrays. Add one exact scalar sin/cos pair owner and call it once for each of the four dynamics
  half-angles, halving function calls and range reductions while preserving the individual scalar
  polynomial results. The same dynamics ownership/deletion boundary applies.
- 2026-08-03 — `rejected`: scalar paired trig preserves both digests but computes both polynomial
  branches for each angle; that costs more than the duplicate scalar reduction it removes.
  Resident-512 candidates lose every reversal (`41,240.4`, `41,148.1`, `40,622.4` cycles/frame
  versus `40,542.0`, `39,912.9`, `39,844.9` controls). Resident-256 candidates are
  `42,911.4`, `41,955.8`, `41,576.5` versus `41,908.3`, `41,581.8`, `41,426.2` controls. The
  helper, declaration, and all four calls are removed. Both exact shared-trig forms are now closed
  for this call shape.
- 2026-08-03 — `open`: compile each immutable dynamics constraint rotation once in
  `lb_80011710`. Final owner is the native runtime `DynamicsData`; canonical source remains
  `unk_58`, which has no post-construction writer. The solver is the only consumer. Native runtime
  fields `rotate`, `unk_6C`, and `unk_78` have no post-construction reader and provide the exact
  nine-float matrix storage without growing the descriptor or snapshot. The compiled matrix
  displaces one `lbVector_CreateEulerMatrix` call per solved link per frame. Construction still
  executes that exact evaluator once, PPC keeps the source representation, and there is no
  allocation, dual mutable state, setter hook, or fallback. Retain only with exact full-domain
  output and controlled gains at both resident sizes.
- 2026-08-03 — `reassessed`: the first exact nine-float matrix layout loses at both sizes. Its
  rows were scattered across `rotate`, `unk_6C`, and `unk_78`, adding hot descriptor/cache-line
  loads and expanding `lb_8001044C`; resident-512 candidates were `39,930.8`, `40,757.7`, and
  `40,123.4` cycles/frame versus `39,623.9`, `40,237.5`, and `40,337.8` controls, while all three
  resident-256 pairs lost. The architectural deletion remains valid, but that storage shape does
  not. Refine it to six exact sin/cos values in contiguous dead `unk_6C`/`unk_78`, reconstructing
  the inexpensive matrix products locally while still deleting every per-frame range reduction
  and quintic trig evaluation. Do not retain the scattered matrix form.
- 2026-08-03 — `reassessed`: the contiguous six-float cache is exact and removes the cache-line
  regression, but the initial implementation is neutral: resident-512 medians are
  `39,843.8 -> 39,854.4` cycles/frame and resident-256 medians are `41,524.0 -> 41,423.0`, with
  mixed pairs. Disassembly shows GCC inlined the complete matrix reconstruction into
  `lb_8001044C`, growing the already large solver from `0x1d4c` to `0x1f56` bytes. Keep the compact
  canonical values, but move their exact matrix/parent product into the existing `lbvector`
  subsystem as a normal external leaf, matching the retained solver's call shape without a
  noinline attribute or build-setting change.
- 2026-08-03 — `rejected`: the external-leaf form restores `lb_8001044C` from `0x1f56` to
  `0x1d58` bytes and remains exact, but it is still slower. Resident-512 medians regress
  `40,133.3 -> 40,554.6` cycles/frame (all but one noisy reversal lose); resident-256 medians
  regress `41,400.2 -> 41,475.7`. Fetching six additional derived values and entering the new leaf
  costs more than rebuilding the source's small approximate trig matrix. The full-matrix and
  compact-trig representations, initialization writes, helpers, declarations, and solver branch
  are all removed. No runtime descriptor byte remains repurposed.
- 2026-08-03 — `open`: screen software staging of the next Match's existing hot roots. The final
  owner, state, scheduler order, and Match-major deletion boundary are unchanged; this adds no
  representation or allocation. The batch loop may prefetch the next Match header, hosted
  contexts, compact pose header, fighter GObjs, and Fighter roots while the current Match runs.
  Retain only if exact digests and controlled comparisons show that hiding arena latency outweighs
  the cache pollution; otherwise remove the hints completely.
- 2026-08-03 — `rejected`: next-Match prefetching preserves both digests and initially wins three
  short resident-512 reversals, but the longer reversed-order screen is only noise-sized there and
  regresses resident-256. At 512, controls/candidates are `40,442.4/40,376.6`,
  `40,215.0/40,038.6`, and `39,998.5/40,071.3` cycles/frame. At 256 they are
  `41,442.3/41,401.3`, `41,347.9/41,973.1`, and `42,505.0/42,111.6`; median cost rises about
  1.3%. A Match step is long enough that these few staged lines do not remain reliably useful,
  and the hints sometimes pollute the current Match. The helper and loop hook are removed.
- 2026-08-03 — `open`: request transparent huge pages for the one contiguous native Match-arena
  mapping. Final ownership remains `msl_memory_map_match_arenas`; canonical Match state, per-Match
  offsets, mapped byte count, source pointers, scheduler order, and save/restore are unchanged.
  Linux on the benchmark host uses THP only for advised mappings, so the existing 256/512-resident
  source traversal otherwise spans tens of thousands of 4 KiB pages. The hint is best-effort and
  unavailable platforms keep the identical ordinary mapping. Retain only if exact output, memory
  locks, and controlled performance at both sizes support it.
- 2026-08-03 — `rejected`: whole-mapping THP is exact but catastrophically slower. The fixed
  native reserve is 3 MiB per Match while ordinary live state is about 609 KiB; advising the full
  768 MiB / 1.5 GiB virtual reservation causes huge-page faults to back sparse reserve surrounding
  each live Match. Resident-512 candidate cost rises to `67,843.1` and `62,458.6` cycles/frame
  against `40,689.5` and `40,927.3`; resident-256 rises to `52,321.5` and `51,588.6` against
  `43,232.1` and `41,903.8`. The hint is removed. A future page-layout change would first have to
  delete sparse per-Match reserve, not advise it wholesale.
- 2026-08-03 — `open`: delete zero-weight fall-pose materialization. Final owner remains the
  existing compact interpolation controller and its source `x8AC_animSkeleton`; canonical timing,
  decoder state, callbacks, and main JObj pose are unchanged. When `ftCo_800CC988` receives exact
  zero, the alternate controller must still advance for a later nonzero blend, but its private SRT
  values and the zero/one blend into the unchanged main pose are unobserved. Add a no-publication
  advance entry to the existing compact owner, with no cache, action table, allocation, fallback,
  or second timing state. Retain only with exact full-domain output and controlled gains at both
  resident sizes.
- 2026-08-03 — `rejected`: the source already makes exact-zero `ftCo_800CC988` a complete no-op;
  advancing its private controller changes both production digests (`75046e348333b63b ->
  ffda1211d9949c8f` at 512 and `e6f2a9b270b4161b -> 5c95cd6fe3751701` at 256). The alternate
  timeline intentionally pauses while its blend weight is zero. The no-publication API and all
  call-site/signature changes are removed; there was no zero-weight materialization to delete.
- 2026-08-03 — `open`: separately request transparent huge pages for the two dense, process-wide
  immutable construction arenas. GameData uses 72.4 MiB of its 96 MiB reservation and native DAT
  uses 90.8 MiB of 256 MiB; both fault sequential dense prefixes once, unlike the rejected sparse
  3 MiB-per-Match mapping. Final owners, addresses, byte contents, sealing, allocation counts, and
  gameplay state remain unchanged. Linux may back the advised dense prefixes with 2 MiB pages;
  unsupported hosts keep ordinary mappings. Retain only with exact outputs, bounded shared-memory
  growth from final-page rounding, and controlled gains at both resident sizes.
- 2026-08-03 — `reassessed`: the combined GameData/native-DAT form is exact but loses two of
  three resident-512 reversals: `40,373.3 -> 40,520.4`, `40,439.7 -> 40,428.7`, and
  `40,496.6 -> 40,703.9` cycles/frame. Remove the native-DAT hint and screen only the tighter
  72.4/96 MiB GameData mapping once; reject the page-size direction if that isolated form does not
  win both resident sizes.
- 2026-08-03 — `rejected`: GameData-only THP also loses every short reversal. Resident-512
  controls/candidates are `40,396.5/40,650.8` and `40,493.4/40,845.3` cycles/frame; resident-256
  pairs are `41,937.6/42,012.6` and `41,930.4/42,392.9`. Page promotion does not help the active
  immutable working set on this host. All memory-advice code is removed; mappings and physical
  behavior return exactly to the retained candidate.
- 2026-08-03 — `open`: batch the native live Euler conversion inside static/action pose blends.
  Final owner is still `ftAnim_80070010` / `ftAnim_80070108` and canonical output is the same main
  or interpolation JObj SRT; consumers and dirty-flag publication are unchanged. Each traversal
  currently calls `lb_8000C868` separately for many independent joints, repeating scalar trig
  setup. Gather only those calls from the existing traversal, evaluate all live half-angle
  sin/cos values through the retained bit-exact wide trig owner, then execute the same authored
  quaternion sign selection, scalar slerp, and JObj publication per joint. Authored/static copies,
  unsupported PPC/Wasm behavior, and early identical-rotation results remain source-shaped. No
  state, table, allocation, fallback, or consumer API is added; remove the batch owner unless exact
  output and controlled whole-runtime gains justify it.
- 2026-08-03 — `retained candidate`: the exact wide blend owner now covers authored/action and
  two-live-JObj fall/transition blends, including wide sine evaluation for the remaining general
  slerps. Three 131,072-frame reversals against the frozen quaternion-only candidate preserve
  digests `a00e2d90ed375c84` / `9c1f37ab084f8ef2`. Resident-256 controls/candidates are
  `41,516.2/40,334.0`, `41,366.1/40,277.4`, and `41,421.0/39,921.5` cycles/frame (median
  throughput `103,618 -> 106,560` FPS, +2.84%). Resident-512 pairs are
  `39,823.5/38,839.7`, `40,342.6/38,711.6`, and `40,184.8/38,810.1` (median throughput
  `106,805 -> 110,588` FPS, +3.54%). The updated profile reduces action-animation callback cycles
  from roughly 274.0M to 212.4M on the same 65,536-frame shape; total diagnostic cost falls from
  about 50.7k to 47.9k cycles/frame. No state or allocation is added. Retain uncommitted; combined
  work has not yet cleared the +5% checkpoint against commit `5fac340b` at both sizes.
- 2026-08-03 — `rejected`: a dedicated AVX-512 sine-only batch kernel for the general quaternion
  slerps preserves both 131,072-frame digests but does not improve the retained wide-sincos form.
  Three committed-control/current pairs have median throughput gains of only +4.02% at
  resident-256 (`42,194.4 -> 40,563.5` cycles/frame) and +4.02% at resident-512
  (`40,823.7 -> 39,301.5` median cycles/frame), below the preceding combined candidate screens.
  The extra trig kernel and API are removed; the exact wide blend remains.
- 2026-08-03 — `open`: tighten the retained blend kernel without changing its operation stream.
  The current stack scratch zeroes all 140 activity bytes on every call, then revisits the authored
  inputs in a second loop solely to load active quaternions. Publish each reached activity byte and
  load its immutable quaternion in the existing first traversal instead. Canonical pose, outputs,
  math order, allocation, and non-native paths remain unchanged; retain only if exact screens and
  controlled measurements show a real cumulative gain.
- 2026-08-03 — `reassessed`: a 32,768-frame resident-512 census covers 421,232 blended outputs;
  every output already has `JOBJ_MTX_DIRTY` before rotation-mode publication and none enters the
  recursive cold flag path. This follows the retained ordering: main/alternate animation dirties
  the live pose before these SRT blends. Make the batch owner publish its final quaternion/dirty
  bits directly, deleting 421,232 redundant dirty tests and unreachable recursive calls from that
  window. Full validation must disprove any supported transition that violates the census; PPC and
  scalar source owners stay unchanged.
- 2026-08-03 — `rejected refinement`: unconditional direct flag publication preserves the digest
  but is 0.33% slower in three resident-512 reversals (`38,248.3 -> 38,375.9` median cycles/frame).
  The perfectly predicted dirty test keeps the exceptional source propagation path cold and gives
  the hot compiler layout better results than deleting it. Restore the measured conditional helper;
  no census or profiler code remains.
- 2026-08-03 — `open`: reassess the authored-quaternion memo now that the retained blend owns a
  wide Euler evaluator. The memo's earlier scalar screen was only +0.13% at resident-512 and adds
  302,848 immutable bytes; append the authored Euler inputs to the existing live batch instead,
  deleting the native-DAT representation and its scattered derived loads. This is the same exact
  conversion and increases only stack-batch width, with no Match state, allocation, or consumer
  change. Retain the simpler form only if it is exact and no slower at both sizes.
- 2026-08-03 — `rejected`: batched authored conversion removes the 302,848-byte memo and remains
  exact, but loses two of three resident-512 reversals. Memo controls are `38,210.8`, `38,182.2`,
  and `38,225.0`; recomputation candidates are `37,945.2`, `38,416.2`, and `38,504.0`
  cycles/frame, raising median cost 0.54%. The authored direct load is restored; adding three more
  exact wide trig blocks per average blend costs more than its scattered immutable footprint.
- 2026-08-03 — `open`: fuse the ordinary no-blend quaternion-clear pass into the existing compact
  main-pose span. The 2026-07-23 attempt replaced recursive clearing with a separate descriptor
  pass and regressed; this form deletes that entire extra walk by clearing each reached JObj just
  before its existing animation operation. Source ordering is retained for root-motion actions,
  which keep the original clear-then-custom-traversal path. The canonical JObj flags/SRT, compact
  node state, callbacks, allocation, and PPC/Wasm paths do not change. Exact full-workload digests
  decide whether the per-node interleave is semantically valid.
- 2026-08-03 — `rejected`: the fused clear/animate form preserves the resident-512 digest but is
  neutral against the retained blend tree. Three 131,072-frame fused/current pairs have medians
  `37,977.6 / 37,963.3` cycles/frame (0.04% in favor of the unchanged path) and mixed individual
  results. Interleaving deletes the extra traversal but does not delete the per-node flag work; the
  helper, API, and scheduling branch are removed completely.
- 2026-08-03 — `open`: batch the remaining exact `acosf` work for general quaternion slerps. Wide
  Euler and sine evaluation leave one scalar PPC-estimate/Newton/`atanf` chain per active joint.
  Gather the existing dot products, evaluate that identical binary32 operation stream in native
  lanes, then publish the same angles and ordered slerp outputs. The live/authored quaternions,
  general-case classification, canonical JObj pose, allocation, and PPC/Wasm scalar paths remain
  unchanged. Do not retain a broad math API or a merely relocated cost: require exact production
  digests and controlled whole-runtime gain at both resident sizes.
- 2026-08-03 — `rejected`: the exact AVX-512 Newton/`atanf` batch preserves the production digest
  but loses all three resident-512 reversals against the retained wide blend. Control costs are
  `37,954.4`, `38,039.8`, and `38,477.0`; batched-acos costs are `38,374.2`, `38,516.6`, and
  `38,400.7` cycles/frame, raising median cost about 0.95%. Scalar PPC reciprocal-estimate calls
  remain necessary and lane classification/gathers outweigh vectorizing the surrounding float
  work. The math API, vector evaluator, and gather arrays are removed completely.
- 2026-08-03 — `open`: share the source camera subject-bounds traversal between the primary and
  offline transform-copy pipelines. Final mutable owners remain the two existing
  `CameraTransformState` values and their independent smoothing histories. The subject list,
  stage-bound queries, common raw min/max bounds, and subject count are identical inputs to the two
  adjacent calls; only final z-dependent padding differs by transform. Compute the common bounds
  once, finalize both outputs in source float order, then run each existing smoothing pipeline in
  its original order. Slippi FreezeDeadUpFall matches already omit the second pipeline. Add no
  state, approximation, cache, or allocation; remove unless the complete digest proves the first
  pipeline has no relevant side effect on the second traversal and both resident sizes improve.
- 2026-08-03 — `rejected`: shared offline camera bounds preserve both digests, but do not improve
  the longer resident-512 control. Quaternion-only controls/current candidates are
  `39,989.6/38,900.1`, `40,122.2/38,949.9`, and `39,865.5/38,662.6` cycles/frame, a +3.01%
  throughput ratio versus +3.54% for the immediately preceding wide-blend tree. The extra paired
  output path and branch offset the traversal saved on only the offline subset. The helper,
  optional output, and call-site scheduling change are removed completely.
- 2026-08-03 — `retained checkpoint`: complete the Dream Land object-1 cut by preserving its
  gameplay-observed `xD0` clear and omitting only the native background-animation request. The
  exact 366-case digests remain `bdff41cf74a54850` / `ee9d93c545aa3ef9`. Three 262,144-frame
  alternating pairs against committed control `5fac340b` improve median paired throughput by
  6.70% at both resident sizes. Resident-256 control/candidate cycles/frame are
  `41,480.0/38,882.2`, `41,683.6/39,065.1`, and `41,529.2/38,875.2`; resident-512 pairs are
  `39,644.6/37,186.9`, `39,886.5/37,318.4`, and `40,249.0/37,721.1`. Candidate median wall FPS is
  110,383 / 115,009. The frozen candidate SHA-256 is
  `461dd9abaafb6205328fcfa0e0c113bc0c4bec277846559becdd8a57b54d8efd`.
  `source-check`, native smoke, PPC smoke, complete 3,501,461-frame supported-domain validation,
  API/test, Wasm, and viewer gates pass. The final profile is 47,078.7 diagnostic cycles/frame;
  pose animation remains 14.62%, stage collision 13.52%, dynamics 7.02%, input/action 4.65%, and
  camera 4.17%. Dream Land object 1 is gone from the top scheduled owners. This qualifies as the
  first >=5% retained checkpoint over `5fac340b`.
