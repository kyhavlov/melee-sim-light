# Performance journal — 2026-07-23

Imported from the historical `/mnt/nvme0/projects/melee-sim-light-decompport` checkout. This is
the unique cumulative optimization journal and authoritative baseline that were not present in the
current branch's performance ledger. No source code or runtime artifacts were imported.

## Cumulative optimization journal — 2026-07-23

The permanent control is detached worktree
`/mnt/nvme0/projects/melee-sim-light-decompport-control-62ba46c5` at exact commit
`62ba46c55b0d9547a9f961ce5d035b4f86c0b15f`. Every result below uses its untouched
strict-release binary, the fixed 153-case manifest, CPU 0, eight warmup ticks, equal
logical/resident counts, matching production digests, and alternating execution order.

### Rejected: batch-native fighter-animation phase seam

The committed candidate was re-profiled before further work: pose animation remains 475.02
million cycles, 13.87% of the 3.430-billion-cycle instrumented contract. Three exact scheduler
layouts then measured the complete real-input seam before replacing any arithmetic. Full-resident
priority grouping added about 20.7% / 17.6% cycles at resident 256 / 512. Target-only grouping of
the real `Fighter_8006A360` owner reduced that to 9.0% / 5.9%. Processing complete source
schedulers in eight-environment tiles still added 5.9% / 6.9%, with matching digests.

The final evaluator would additionally have to split and resume each fighter callback around pose
animation, increasing canonical Match transitions. It would therefore need to remove roughly
two-thirds of the complete animation owner merely to recover the seam and produce the remaining
whole-runtime target. Both serious layouts were removed, `src/runtime/batch.c` is byte-identical to
the committed checkpoint, and the standard binary SHA-256 is again
`7f60c6b97057dd333ffad3af2e7ab1e07cc311d7209a698242674d20fc56c781`.

### Retained: direct canonical fighter-animation spans

The existing `MslFighterPoseJoint` preorder is now the bulk traversal owner. Main fighter pose
nodes bind once to their source `FighterBone` index during construction. Full-tree and
part-subtree animation consume the contiguous node span directly, deleting per-node public-wrapper
lookup plus `ftAnim_8006E7B8`'s repeated JObj DFS and source-part search. The existing Fighter
timeline, decoder cursors, canonical JObj SRT/dirty flags, source part flags, callback lifetime,
RObj work, and scheduler cut remain unchanged.

The initial 65,536-match-frame alternating screen produced:

- resident 256 control cycles:
  `2803163249, 2795338496, 2797906972`; candidate:
  `2732403309, 2720025071, 2740062942`; medians
  `2797906972 -> 2732403309` (**-2.34% cycles**).
- resident 512 control cycles:
  `2983078904, 2983039989, 2972623153`; candidate:
  `2908220763, 2900266666, 2899291813`; medians
  `2983039989 -> 2900266666` (**-2.78% cycles**).

Digests remain `8ef126a41244d514` / `6f91f23e3553a090`. The complete 153-replay gate remains
63 PASS / 90 unchanged CLASSIFIED / zero failures. `MslFighterPoseJoint` does not grow: the cold
part index occupies previously unused descriptor bits. There is no new mutable state, shared data,
allocation, alternate evaluator, admission path, or runtime mode.

### Rejected: contiguous fighter quaternion-flag clearing

The recursive fighter-root `HSD_JObjClearFlagsAll` pass was replaced with a loop over the existing
pose-node span while retaining `HSD_JObjClearFlags` and exact digests. Two short alternating
screens were neutral at resident 256 and about 3.3% slower at 512. The source operation can dirty
complete dependent subtrees for classical-scale nodes, so the candidate added a second cold
descriptor walk without eliminating that required JObj traversal. The function, declaration, and
call-site substitution were removed completely.

### Rejected: cold dynamic-hurt ownership binding

A source-backed bit on the existing pose descriptor replaced
`ftCo_HeadlessPublishDynamicHurtCapsules`'s per-frame hurtbox × retained-dynamics-chain search with
one direct membership test per hurtbox. It preserved both production digests and left the demanded
capsule transform, skip lifetime, contact state, allocation, and representation unchanged. Despite
removing 25.49 million self instructions in the diagnostic window, alternating standard binaries
were neutral/noisy at resident 256 and did not improve resident 512; one 512 pair was slower.
The relationship bit, bind pass, and direct publisher were removed completely. The required
world-transform publication dominates this narrow owner, so it is not a useful standalone cut.

### Retained: authored-node shield-pose blending

Disposable callback-target attribution split the 9.75% action-animation aggregate and found the
ordinary shield family dominant: `ftCo_Guard_Anim`, `ftCo_GuardOn_Anim`, and
`ftCo_GuardReflect_Anim` together consumed 175.5 million cycles in the 65,536-frame resident-512
profile. Within their source-owned directional-pose builder, `ftAnim_80070108` consumed 80.44
million cycles (2.24% of the complete contract). The source path immediately resets the complete
interpolation skeleton to the static authored pose, applies the shield Figa only to joints with
attached tracks, and then blended that same static pose back into every joint.

The native canonical path now performs the static-to-animated blend only for joints that actually
carry the attached Figa. Those joints retain the exact source `lb_8000C868` operation order;
unattached joints already contain the exact static result and perform no duplicate quaternion/SRT
work. The same source-shaped precondition also covers Sheik's existing directional-chain pose.
There is no second pose owner, action/character list, new state, shared data, allocation, or
admission mode. The measured `ftAnim_80070108` owner falls to 33.89 million cycles and the complete
action-animation callback bucket falls by about 46.6 million cycles.

A state-free attached-only animation traversal was also tested after this cut. It cost 45.15
million cycles against the source traversal's 38.67 million because required dependency-dirty
propagation still spans the physical tree. It was removed completely.

### Cumulative +5% checkpoint

The first strong cumulative checkpoint retains only direct canonical fighter-animation spans and
authored-node shield-pose blending. It uses the permanent control at exact commit `62ba46c5`, the
fixed manifest, strict release flags, CPU 0, eight warmup ticks, 524,288 match-frames, and
alternating execution order. Every pair has matching production digests.

- resident 256 control cycles:
  `22301387431, 22790946344, 22745043715, 22655264445, 22662956973`;
  candidate:
  `21263653553, 22084756355, 21465510646, 21382715952, 21442798261`.
  Medians are `22662956973 -> 21465510646` (**-5.284% cycles, +5.579% FPS**).
- resident 512 control cycles:
  `24744673790, 23907437732, 24920188739, 25030764615, 24400207283`;
  candidate:
  `22976390337, 23007727834, 23297096334, 23398887525, 22659131693`.
  Medians are `24744673790 -> 23007727834` (**-7.020% cycles, +7.549% FPS**).

The full gate remains 63 PASS / 90 unchanged CLASSIFIED / zero failures across 1,415,476 frames,
and all 38 tests pass. Both retained cuts preserve allocation, mutable state, shared data, and
snapshot size.

### Retained: Dream Land visual-response callback deletion

Dream Land map object 7 remains the canonical Whispy timer, direction, wind, and RNG owner. It
writes presentation mirror fields on map object 1, whose priority-4
`grOldPupupu_80210A24` callback only selected and reset visual Figa animations; those values are
not read back by gameplay. The existing construction-owned stage schedule mask now removes that
single callback. Map object 2's transient clouds, map object 7's gameplay state, RNG, replay
events, and every collision epoch remain scheduled. This adds no runtime branch, mutable state,
shared data, or allocation.

Against the exact cumulative parent binary
`ef96b71320e30f2e2a9dbaa19f02988201f232937c915e0d6e8f1b631b8b59b8`, a
262,144-match-frame three-pair alternating screen produced:

- resident 256 parent cycles:
  `10609682744, 11102294657, 10748761084`; candidate:
  `10658527476, 10597539802, 10761952323`. Medians are
  `10748761084 -> 10658527476` (**-0.839% cycles, +0.847% FPS**).
- resident 512 parent cycles:
  `11900107154, 11695636435, 11868313470`; candidate:
  `11676610526, 11454339398, 11864638002`. Medians are
  `11868313470 -> 11676610526` (**-1.615% cycles, +1.642% FPS**).

Digests match at `98a22941608f991d` / `ab19c61ab7772393`. The Dream Land validation subset remains
6 PASS / 14 unchanged CLASSIFIED / zero failures across 224,998 frames.

### Rejected: common-action O2 closure

The previously exact eight-file common-action O2 set was re-screened because its former
three-point retention threshold no longer applies. Against the exact cumulative parent, resident
256 controls measured `2,625,166,469`, `2,633,982,286`, and `2,636,228,520` cycles while the first
two candidates measured `2,628,645,900` and `2,671,188,079`: neutral once and materially slower
once. A second distinct hot-only set (`Guard`, `Fall`, `Wait`, and `Jump`) was slower in both a
resident-256 pair (`2,769,687,233` versus `2,639,714,229`) and a resident-512 pair
(`2,918,780,101` versus `2,844,101,141`), with matching digests. The native optimizer's code-size
and layout effects outweigh its local callback gains at scale. The build allowlist is fully
restored; no compiler experiment remains.

### Rejected: static-stage collision-epoch suppression

A construction-owned experiment kept all required stage animation, callbacks, and RNG work but
stopped advancing `mpColl_804D64AC` on FD, Battlefield, frozen Stadium, and Dream Land. The result
failed immediately and broadly: even FD first diverged when fighters approached stage edges, and
the complete suite produced 90 failures. `CollData.x38` is therefore a semantic source-generation
boundary, not merely a moving-platform transform cache; selecting the stable query path does not
preserve source floor/ledge lifetime. The new processes, stage metadata, and every call-site change
were removed before timing.

### Rejected: frame-major direct Figa samples

The existing immutable exact sample values were transposed from node-major to program/frame/node
order while retaining the same value count, 16-byte program-node descriptor, publisher, and
digests. Resident 256 was repeatably about 0.4--0.6% faster, but resident 512 alternated between
`2,771,579,534` candidate versus `2,767,850,660` parent cycles and `2,760,329,702` versus
`2,762,562,348`: noise-bound with one loss. Shared-value locality is not the scale bottleneck on
the current evaluator. The layout, stride field, and cold compiler changes were removed.

### Rejected: already-dirty dependency-call guard

Guarding `HSD_JObjCheckDepend` with the exact dirty bit preserved both digests and looked positive
in short 65,536-frame screens. Two 131,072-frame resident-256 reversals disproved it: candidate
cycles were `5,136,410,028` and `5,132,419,241` against parent `5,116,875,171` and
`5,106,902,783`. The extra branch costs more than the cold calls it avoids. The guard was removed.

### Rejected: unreachable hosted AObj end-callback accounting

The linked hosted source has no writer for the private AObj end-callback list, so a final-shaped
candidate removed its per-node ended/active counts, TLS context, invoke/reset calls, and 16 bytes of
GameData/Match state while leaving all animation state and writes exact. The production digests
matched, but longer resident-256 reversals were neutral once and slower once:
`5,157,772,213` candidate versus `5,110,817,245` parent, then `5,157,098,661` versus
`5,156,417,627`. The source/context layout and all accounting were restored exactly; correctness
alone does not justify a scale regression during this throughput packet.

### Five-hour cumulative endpoint

The final cumulative candidate retains only direct canonical fighter-animation spans,
authored-node shield-pose blending, and the Dream Land visual-response callback deletion. The
fixed control is exact commit `62ba46c55b0d9547a9f961ce5d035b4f86c0b15f`; control/candidate
binary SHA-256 values are
`b6c7e4ef988609774f43799d22c507ab61ecf6b13f1a04a5ce556e83c7b4191f` and
`7f60c6b97057dd333ffad3af2e7ab1e07cc311d7209a698242674d20fc56c781`.
Five alternating pairs used the fixed 153-case manifest, strict release flags, CPU 0, eight warmup
ticks, equal logical/resident counts, and 131,072 match-frames.

- resident 256 control cycles:
  `5,449,922,899, 5,440,311,926, 5,464,323,728, 5,464,511,509, 5,462,713,937`;
  candidate:
  `5,126,151,905, 5,129,322,381, 5,121,707,597, 5,114,995,125, 5,143,877,021`.
  Medians are `5,462,713,937 -> 5,126,151,905`
  (**-6.161% cycles, +6.566% FPS**).
- resident 512 control cycles:
  `5,814,893,827, 5,831,549,146, 5,806,927,475, 5,802,969,841, 5,823,204,394`;
  candidate:
  `5,441,109,834, 5,456,164,650, 5,454,730,729, 5,411,653,931, 5,486,823,521`.
  Medians are `5,814,893,827 -> 5,454,730,729`
  (**-6.194% cycles, +6.603% FPS**).

Digests match at `c893ea5f3272c026` / `4e394b9c1b5a5bf2`. The complete replay gate is
63 PASS / 90 unchanged CLASSIFIED / zero failures across 1,415,476 frames; all 38 tests and
source/native/PPC/Wasm/viewer checks pass. Runtime allocation remains sealed
(`631,820` arena bytes and `824` allocations before/after stepping). `MslCoreMatch`,
`MslCoreGameData`, and `MslFighterPoseJoint` remain exactly 62,024, 371,792, and 56 bytes; there is
no mutable/shared-data or snapshot growth. The candidate text section is 1,200 bytes smaller
(`1,430,731` versus `1,431,931`).

The session therefore ends below the requested +10% target but with a repeatable, correctness-green
+6.6% cumulative result. The scalar owner sweep also shows diminishing returns: the next credible
gain requires a bounded batch-native animation/data-layout packet, not further leaf specialization.

### Rejected: direct match-owned observation export

Observation and terminal serialization consume 3.04% of the profiled public contract and entered
the same match context up to three times to expose stock and item owners. Removing that context
publication entirely increased observation from 96.89M to 102.90M cycles because the direct
per-match owner access displaced the hot TLS view. A refined version retained the one canonical
observation bind and removed only the item writer's immediate same-match rebind plus terminal's
read-only rebind. That saved 3.66M instrumented cycles, just 0.11% of total runtime, and its
131,072-frame standard screen was positive at resident 256 but 1.2% slower at 512. All direct-read
and bind-deletion changes were removed.

### Rejected: fused authored-node alternate-pose sampling

The guard/Sheik directional-pose path attributes 39.20M cycles (1.13% of the contract) to its
full-tree animation scan, alongside 13.27M for Figa attachment, 5.69M for static reset, and 2.45M
for frame request. A fused implementation collected physical authored JObjs during the existing
Figa-to-part pass, then requested and animated only that list in source order while retaining the
same decoder state, static reset, and downstream blends. Both production digests matched. The
first short screen improved resident 256 by 0.94%, but reverse order was 1.15% slower; resident
512 was consistently 1.62--2.48% slower. The stack-list working set is worse than the existing
contiguous traversal at scale, so all helper, call-site, and diagnostic changes were removed.

### Rejected: fall blend attachment-union cull

The common fall callback tested blending only joints carrying animation on either the main or
alternate fall pose. Both production digests remained exact and resident 256 improved about
2.6--3.0% in two immediate-parent pairs, but resident 512 was neutral/noisy with one materially
slower pair. The extra attachment queries add another descriptor stream to a cache-sensitive full
pose blend, so the helper, dispatch, and call-site change were removed completely.

### Rejected: exact dense-sample deduplication

The cold Figa compiler byte-deduplicated complete node sample sequences and compacted its immutable
float array from 10,718,160 to 8,550,985 values, saving 8,668,700 bytes (20.2%) with exact
production digests and no runtime branch or state. Standard binaries were neutral/noisy at 512 and
had two slower of three immediate-parent pairs at 256. The duplicate sequences are not the active
working set on this workload, so the compactor and temporary construction hash were removed rather
than retaining memory-only complexity during the throughput session.

### Rejected: action-specific stage-collision specialization

Disposable `Fighter_procMap` target attribution split the 13--14% collision-callback aggregate
across landing, wait, aerial, damage, dash, jump, and smaller action callbacks. The dominant
targets all converge on the same common mpColl ECB load and query families; no target exposes a
separate repeated query or traversal. Existing complete-owner attribution already assigns the
dominant cost to `mpColl_LoadECB_JObj`, whose direct-query, sparse-closure, eager-matrix, and batch
publication alternatives were exact but neutral or slower. The target instrumentation was removed
without adding action dispatch, another line admission, collision cache, or second ECB owner.

### Rejected: repeated interpolation-Figa attachment reuse

`ftAnim_8006F4C8` costs 74.89M profile cycles across 17,770 calls, including 2,882 repeated
guard/Sheik attachments. A complete mapping scan before attachment was exact but neutral/slower at
256 and noisy at 512. A distinct no-scan version recorded the canonical full-tree program in unused
controller flag bits and eliminated those 2,882 calls, but attachment cycles fell only 4.03M and
standard binaries became materially slower at both resident sizes. The already fixed storage makes
repeat attachment comparatively cheap; both identity designs, state, APIs, and call-site changes
were removed.

### Rejected: external action-pose descriptor traversal

The remaining guard, fall, and Sheik `HSD_JObjAnimAll` calls consume 38.67M profile cycles outside
the `ftanim.c` compact macro boundary. Routing them through the existing contiguous
`MslFighterPose` span preserved both production digests, but the quick standard screen regressed at
both 256 and 512. These complete-tree consumers make the physical JObj DFS cheaper than touching
the separate descriptor and JObj streams. The wrapper and every call-site change were removed.

### Rejected: inlined compact-pose dependency check

Inner-owner timing attributes 121.72M net cycles across 6,488,168
`HSD_JObjCheckDepend` calls. Copying its exact source predicate into the compact pose translation
unit removed that call boundary and preserved both digests, but was neutral at resident 256 and
materially slower at 512. Inlining expands the hottest node loop while retaining every required
dirty-propagation test. The duplicate predicate was removed and the singular HSD owner restored.

### Rejected: optimized transient hit-state reset

Although `Fighter_ProcessHit_8006D1EC` owns 2.2--2.7% of profiled runtime, disposable timing assigns
only 3.07M cycles (0.09%) to its unconditional transient damage/shield/reflect field-reset tail.
The cost is in demanded hit-resolution behavior, not scalar clears, so no helper, range clear, or
dirty bit was added. Input/IASA attribution was also fragmented: the largest callback was dash at
0.66% and no complete shared deletion boundary appeared. All instrumentation was removed.

### Rejected: shared fighter-input preprocessing rewrite

The complete scheduled input owner consumes 9.13% under inner instrumentation, but the
action-specific `input_cb` consumers account for 5.04%. The remaining source-ordered canonical
preprocessing is already fragmented into pad acquisition/deadzone/button publication (20.82M
cycles, 0.58%), stick recurrence and UCF publication (19.40M, 0.54%), trigger/button recurrence
(8.95M, 0.25%), and common maintenance (15.65M, 0.44%). No coherent region has enough deletable
work to justify rewriting exact UCF and input recurrence state, and the earlier complete hosted
human-input specialization plus whole-function compiler variants were neutral or slower. All
inner timing is removed; no input state, helper, or alternate path remains.

### Rejected: fighter animation scheduler-shell rewrite

After excluding the retained Figa/JObj evaluator and action animation callback,
`Fighter_8006A360` has no large residual owner. Disposable attribution split the remainder into
frame timers and uncommon status maintenance (19.38M cycles, 0.57%), pre-animation counters
(7.44M, 0.22%), post-animation script/collision bookkeeping (14.39M, 0.42%), and the canonical
player-nudge/color-animation tail (26.87M, 0.79%). These are separate persistent-state owners, not
one traversal or presentation-only boundary. All timing is removed rather than merging them into a
speculative shell rewrite.

### Rejected: second fighter-contact broad phase

Disposable callee attribution splits `Fighter_8006CB94` into fighter-hit/hurt contact (45.46M
cycles, 1.30%), shield contact (23.65M, 0.68%), and seven independent grab, item, push, reflect,
absorb, reset, and rumble families of 0.07--0.23% each. The dominant fighter contact path already
retains the empty-producer cull and exact per-capsule AABB rejection. A higher-level reject would
need another derived hurt bound or duplicate exact capsule transforms, violating the single-owner
criterion for at most a 1.30% ceiling. All callee instrumentation is removed and no second
collision cache or broad phase was added.

### Rejected: mutually exclusive observation zero owners

Both public observation writers were tested with one prefix clear for header, stage, and player
slots plus the existing item-array clear, deleting the complete-record/item overlap and nested
player clears. Digests remained exact, but immediate-parent 65,536-frame pairs were slower at both
resident sizes: 256 rose from 2,614,969,664 to 2,641,431,305 cycles and 512 from 2,744,161,358 to
2,774,074,351. The optimized single wide clear provides better cache/store publication than the
smaller split clears despite writing duplicate bytes. The original complete and local zero owners
are restored; no serializer or output-layout change remains.

## Authoritative canonical-runtime baseline — 2026-07-23

Branch `experiments/decomp-port/optimizations` begins at exact commit
`a5f699e768d9f422d6c00204546f1384ee5bafcb`. The only setup delta is Make dependency
fingerprinting for Python compilation and `RELEASE_ARCH_FLAGS`; it changes no production
preprocessor definition, compiler flag, source, state, or runtime behavior.

Two fresh strict-release builds were made from the same working directory in separate clean build
roots before and after that Makefile delta. Raw ELF SHA-256 differs because the build-directory
path changes DWARF and the GNU build ID:

- untouched reference: `edda3056bbefb4c456fd1d53eda169e8513a8e46920bd933bc276202680dd2be`
- prepared branch: `ca5a3e56fc1caf627d61a729992162d63da39373aa3ef41ed7a3f162990f8e96`

After stripping debug sections and `.note.gnu.build-id`, both complete runtime images are
byte-identical with SHA-256
`c8d1458e779d315c21d6f93f7da762da3f78f3de8d9de2521d016d3032f5ac00`. Both have 1,430,075
text bytes, 96,294 data bytes, and 732,680 BSS bytes. This establishes production-binary neutrality
without relying on throughput noise.

### Production benchmark

- Host: AMD Ryzen 9 9950X3D; CPU affinity: CPU 0.
- Compiler: GCC 13.3.0; repository strict-release O0/O1/O2/O3 owner profile,
  `-march=native -mtune=native`, no LTO or unsafe floating-point flags.
- Generated 153-case manifest SHA-256:
  `e6051de985c16c1053de1b9a52299d1250cf8da47bd8231fd4183055c0b6a5ec`.
- Workload: equal logical/resident counts, eight warmup ticks, 524,288 match-frames, 128-frame
  observation history, startup stagger `8 x 100` after a 200-frame base.
- The following five runs at each size are unfiltered and retain execution order.

| Resident | Run | Cycles | Cycles/frame | FPS | Digest |
| ---: | ---: | ---: | ---: | ---: | --- |
| 256 | 1 | 21,871,206,143 | 41,716.0 | 102,885 | `ec0faefa8c71adde` |
| 256 | 2 | 21,940,898,608 | 41,848.9 | 102,558 | `ec0faefa8c71adde` |
| 256 | 3 | 22,112,748,409 | 42,176.7 | 101,761 | `ec0faefa8c71adde` |
| 256 | 4 | 21,986,259,437 | 41,935.5 | 102,347 | `ec0faefa8c71adde` |
| 256 | 5 | 22,151,149,989 | 42,250.0 | 101,585 | `ec0faefa8c71adde` |
| 512 | 1 | 25,147,343,248 | 47,964.8 | 89,481 | `dadef5c5e5aadd94` |
| 512 | 2 | 25,052,759,932 | 47,784.3 | 89,819 | `dadef5c5e5aadd94` |
| 512 | 3 | 23,994,760,928 | 45,766.4 | 93,780 | `dadef5c5e5aadd94` |
| 512 | 4 | 23,644,972,741 | 45,099.2 | 95,167 | `dadef5c5e5aadd94` |
| 512 | 5 | 23,435,031,433 | 44,698.8 | 96,019 | `dadef5c5e5aadd94` |

The unfiltered medians are **21,986,259,437 cycles / 102,347 FPS at resident 256** and
**23,994,760,928 cycles / 93,780 FPS at resident 512**. The monotonic improvement during the 512
series is retained rather than filtered; future checkpoints use alternating parent/candidate pairs
to cancel this host warmup/frequency movement.

### Correctness and memory

The complete release suite is 63 PASS / 90 unchanged CLASSIFIED / zero XPASS, failure, or error
over 1,415,476 frames. All 38 tests pass, including sealed allocation and arbitrary-index
copy/save/restore. Source, native, PPC, Wasm, and live browser-viewer gates are green. Wasm state
and viewer digests are `cef96ff32edfe898` and `1b43d24b47c228f2`; snapshots are 508,140 bytes.

The runtime census reports:

- `MslCoreMatch`: 62,024 bytes; source-owned match structures: 35,040 bytes.
- Ordinary arena: 631,820 bytes and 824 allocations before and after stepping.
- Complete savestate: 693,972 bytes, including its 128-byte header.
- Maximum supported construction: 960,000 arena bytes, 2,045 allocations, and four players.
- Shared `MslCoreGameData`: 371,792 bytes; GameData arena 42,117,736 used / 67,108,864 reserved;
  native DAT 51,030,048 used / 268,435,456 reserved.
- Observation history: 996 bytes/frame, 127,488 bytes/environment at 128 frames.

### Fresh owner profile

A 65,536-match-frame resident-512 profile uses a 3,572,582,631-cycle complete-contract
denominator and digest `6f91f23e3553a090`. The dominant measured closures are fighter animation
605,975,909 cycles (16.96%), fighter map/stage collision 465,000,538 (13.02%), action animation
callbacks 349,517,803 (9.78%), fighter dynamics 280,422,350 (7.85%), input/action callbacks
184,893,722 (5.18%), camera callbacks 136,375,661 (3.82%), and observation/terminal publication
108,851,877 (3.05%).

A separate Valgrind-compatible `x86-64-v3` Callgrind window is diagnostic only. It confirms
`msl_fighter_pose_animate_joint` as the largest self owner: 143,090,350 instructions (9.17%),
2,973,554 L1-D read misses (20.07%), and 299,487 L1-D write misses (13.97%). The bulk callers issue
987,644 part-subtree and 106,296 full-tree node calls over 16,384 match-frames.
`ftAnim_8006E7B8` adds 44,368,097 instructions (2.84%) before child work. The selected bounded
owner and its deletion boundary are preserved in [`JOURNAL_2026-07-24.md`](JOURNAL_2026-07-24.md);
Callgrind results do not replace the standard production baseline.

### Current corrected profile

The sibling checkout's corrected baseline additionally recorded that the scheduler owned 93.90%
of instrumented time on the staggered resident-512 workload. Its largest inclusive callback owners
were hosted fighter maintenance (`Fighter_8006A360`, 29.12%), fighter map collision
(`Fighter_procMap`, 13.77%), fighter dynamics (`Fighter_8006D9AC`, 7.94%), Spaghetti input/IASA
(7.50%), and camera (3.79%). Cross-cutting phase attribution assigned 16.84% to fighter animation,
14.75% to pose animation, 12.92% to stage collision, 9.64% to action animation callbacks, and
5.08% to input/action callbacks. Its 80,330 profiler FPS included diagnostic overhead and was not
a throughput baseline; nested rows must not be added to their enclosing phase shares.
