# Performance experiment journal — 2026-07-24

Imported from the historical `/mnt/nvme0/projects/melee-sim-light-decompport` checkout's
`ACTIVE_WORK.md`. This is an archival snapshot: labels such as `open` describe the state of that
old session, not current work. Later disposition evidence elsewhere in this directory takes
precedence. No source code or runtime artifacts were imported.

`reports/triage/`.

## Active packet: dynamic hurt publication binding

The fixed control remains exact commit `62ba46c55b0d9547a9f961ce5d035b4f86c0b15f`.
The immediate parent is exact committed checkpoint
`72419132e77ac3083c2d4e88e475ea425d1c6707`. It retains three exact cuts and measures
+6.57% / +6.60% production FPS at resident 256 / 512 against the fixed control. Complete evidence
and rejected scalar experiments are in `PERFORMANCE.md`.

The dirty tree provisionally retains two exact compiler-owner cuts. Dynamics compiles its
source-identical rotation conversion in the existing O3 solver. Quaternion slerp alone uses O2
within the otherwise required-O0 quatlib owner. Both retain canonical state and add no data or
allocation. Long direct parent/candidate pairs confirm a positive combined result, but the
fixed-control cumulative target remains below +10%.

### Owner map

- **Final owner:** source `FighterHurtCapsule` records.
- **Canonical state:** unchanged hurt capsule offsets/positions/skip lifetime and dynamics JObjs.
- **Consumers:** eager dynamic-hurt publication before fighter contact.
- **Displaced work:** per-frame hurt × dynamics-chain identity search.
- **Deletion condition:** cold dynamics pruning marks the existing unused hosted hurt bit from the
  same source graph; hot publication iterates hurt capsules once in the original order.

### Acceptance shape

Preserve exact publication order and retain only if standard binaries improve at both sizes.

### Immediate next action

Bind dynamic hurt ownership on the consumer record, then run exact and production screens.

## Active log

- **2026-07-24 — rejected:** batch-native animation, full-resident and tiled phase layouts. Exact
  digests proved scheduler equivalence, but canonical-context/cache seam costs consume
  5.9--20.7% of the complete runtime before evaluator replacement. A split evaluator would add
  another transition and need to delete roughly two-thirds of animation merely to recover the
  seam. Disposition: all implementation removed; do not build this seam.
- **2026-07-24 — open:** fighter dynamics. Fresh profile assigns 284.05M cycles / 8.30% to
  `Fighter_8006D9AC`. The retained scalar solver already fuses demanded transforms; prior census
  assigns about 67% to exact orientation/constraint math and 23% to setup/floor work. Next:
  measure an owner-specific batch phase and reject it unless the complete seam plus kernel has
  credible headroom.
- **2026-07-24 — open:** a bounded portable-ISA Callgrind sample attributes 16.35M instructions
  to the real dynamics solver: 3.57M are the O0 quaternion conversion chain, 3.21M basis building,
  2.42M angle evaluation, 1.97M floor queries, and 1.25M axis rotations. Hypothesis: preserve the
  source expression order while compiling only the dynamics quaternion chain inside its O3 owner;
  this deletes avoidable O0 call/stack execution without adding state or another evaluator.
- **2026-07-24 — retained:** the dynamics-only optimized conversion chain preserves production
  digests. Three short pairs reduce raw-median cycles from 2,595,592,273 to 2,559,400,807 at 256
  (-1.39%) and 2,762,648,520 to 2,704,421,747 at 512 (-2.11%). The third 512 pair was externally
  disturbed in both arms; the other two paired reductions are 1.74% and 2.65%.
- **2026-07-24 — open:** quaternion interpolation. The exact O0 slerp remains a large leaf under
  the retained shield/action-pose owner. Whole-file quatlib optimization is documented as
  inexact; test only this singular canonical function before considering any source rewrite.
- **2026-07-24 — retained:** singular O2 admission of `HSD_QuatLib_8037EF28` preserves both
  production digests. Against the dynamics-only tree, three short-pair raw medians reduce cycles
  2,601,679,439 to 2,569,034,828 at 256 (-1.25%) and 2,766,725,995 to 2,736,551,820 at 512
  (-1.09%); median paired FPS gains are +1.27% / +0.72%. The rest of quatlib remains O0.
- **2026-07-24 — rejected:** replacing the local dynamics conversion with optimized shared quatlib
  functions is not exact: O2 `EulerToQuat` changes the resident-512 production digest. The other
  shared functions do not serve the native dynamics call site and add no useful deletion. All
  attempted annotations except the independently exact retained slerp are removed.
- **2026-07-24 — open:** disassembly proves why the shared Euler candidate failed: GCC combined
  each source `cosf`/`sinf` pair into host `sincosf`, a known non-exact transformation. Test the
  same optimized canonical function behind explicit non-builtin scalar trig call boundaries.
- **2026-07-24 — rejected:** an exact O2 Euler function with six explicit scalar trig calls is
  still slower: the two valid 512 pairs add 0.24% and 0.50% cycles, and a clean 256 pair adds
  0.85%. The aliases, annotation, and all experiment code are removed; the O0 owner remains.
- **2026-07-24 — rejected:** O3 for the retained singular slerp produces byte-identical function
  code and size to O2, so there is no distinct optimization. O2 remains the documented level.
- **2026-07-24 — rejected:** `Fighter_8006C80C` is fragmented. Its complete 1.49% phase divides
  among contact publication (0.67% under attribution), effect flush, model transform, accessory
  callback/animation, and magnifier history; no remaining coherent subowner reaches one point or
  exposes dead work. All five timers and buckets are removed.
- **2026-07-24 — rejected:** inlining the nine exact `lbVector_Angle` calls into the dynamics
  evaluator is neutral at 512 and alternates at 256. Exact square-root/acos work remains live, so
  the helper and every call-site substitution are removed.
- **2026-07-24 — retained pending final checkpoint:** three 131,072-frame direct pairs compare the
  combined dynamics/slerp tree with immediate parent `72419132`. Resident 256 parent/candidate
  medians are `5,274,032,474 -> 5,183,970,651` cycles (-1.71%); two of three pairs win. Resident
  512 medians are `5,719,508,045 -> 5,434,792,016` (-4.98%); all three pairs win. Digests match.
- **2026-07-24 — open:** `interpret_joint` re-resolves the same TLS `MslFighterPose` once per pose
  node even though all tree/part traversals already bind it. Pass that canonical owner through the
  existing internal calls; this changes neither state nor semantics and deletes repeated context
  access from the dominant animation traversal.
- **2026-07-24 — rejected:** passing the already-bound pose pointer through `animate_node` and
  `interpret_joint` preserved both digests but slowed the immediate cumulative parent from
  `2,600,702,995 -> 2,616,752,186` cycles at resident 256 and
  `2,798,154,007 -> 2,805,307,702` at resident 512. The native local-exec TLS load is cheaper than
  the additional hot argument/register pressure. The entire source change is removed.
- **2026-07-24 — open:** Callgrind attributes 1.36M libc instructions plus 408K call-site
  instructions in its bounded sample to the fortified 12-byte `memcpy` used to gather each dirty
  Euler node for exact ECB trig. Replace only that fixed gather with its three direct scalar
  assignments; the trig batch, ordering, matrices, and collision packet remain unchanged.
- **2026-07-24 — rejected:** direct scalar ECB Euler gathers remove the fortified call and preserve
  both digests, but lose both resident-256 pairs (`2,605,453,205 -> 2,611,470,410` and
  `2,612,713,239 -> 2,635,507,410` cycles). The one 512 reversal wins, so this is a code-layout/
  pressure trade rather than a reliable complete-runtime cut. The source change is removed.
- **2026-07-24 — open:** Battlefield map object 3 owns a 2,400--3,599-frame background timer and
  exact transition RNG, but its priority-1 source proc traverses the presentation JObj tree every
  waiting frame. The existing state machine can make source animation canonical only during
  `BG_Transitioning`; no additional timer or mirrored controller is required.
- **2026-07-24 — rejected:** making Battlefield object 3's source animation proc live only during
  `BG_Transitioning` preserves the full 153-replay gate, exact timer/RNG/AObj lifetime, and both
  production digests. It nevertheless loses the 131,072-frame reversals at both sizes:
  `5,146,937,331` versus parent `5,138,916,584` cycles at 256 and `5,522,437,540` versus
  `5,439,890,483` at 512. Construction/allocation-layout effects outweigh the sparse stage-local
  deletion. The proc helper, state transitions, and schedule-mask change are removed.
- **2026-07-24 — open:** the earlier dynamic-hurt classification touched a separate pose-descriptor
  stream and was neutral. Callgrind still assigns 2.80M self instructions to the source nested
  search. Bind the same source-derived membership directly on the hot hurt consumer's otherwise
  unused hosted bit, preserving hurt-order publication and adding no bytes.
